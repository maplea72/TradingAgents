"""TradingAgents HTTP API server.

Exposes the agent graph over FastAPI so a remote LLM can drive analyses
through tool calls. See ``server/main.py`` for the route surface and
``server/README.md`` for deploy + integration steps.
"""
